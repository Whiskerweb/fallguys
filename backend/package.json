{
  "name": "fallguys-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Comptes, grand livre, depots et retraits USDC sur Robinhood Chain. Le jeu ne connait aucun solde ; ce service est le seul a en tenir.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node test/tout.mjs && node test/retraits.mjs && node test/adresses.mjs && node test/chaine.mjs && node test/boutique.mjs",
    "tresorerie": "node outils/tresorerie.mjs --ecrire",
    "contrats": "node outils/contrats.mjs",
    "contrats:compiler": "node outils/contrats.mjs --compiler --etat",
    "purger": "node outils/purger.mjs",
    "cycle": "node outils/cycle.mjs",
    "cycle:local": "node outils/cycle.mjs --local"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4",
    "bs58": "^6.0.0",
    "ethers": "^6.17.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
